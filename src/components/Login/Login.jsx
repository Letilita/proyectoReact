import { commonlogin } from "../../firebase/providers/providers";


export const Login =()=>{

/*    function checkForm(e) {

        e.preventDefault()
        wait();
        const user= commonlogin(form.name, form.pass, error)
    }

    async function googleSignIn(e){
        e.preventDefault()

    }
 */ 

    return(

        <div>Login</div>

    ) 
    
}